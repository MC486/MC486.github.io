"""Market sandbox: a lookahead-safe backtesting engine that treats a market as a
turn-based game against a (future) learning strategy ensemble.

The package reuses the architectural "bones" of an event-driven engine, a
pluggable strategy layer, and a repository persistence layer.
"""

__version__ = "0.1.0"
