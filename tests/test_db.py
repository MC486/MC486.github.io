test_schema_creation_integrity() #Verifies that all expected tables and columns are created via schema.sql.

test_insert_and_query_word_history() #Ensures game history can be inserted and retrieved for AI learning.

test_insert_player_and_session_tracking() #Validates player tracking and session ID associations.

test_letter_set_storage_and_retrieval() #Confirms shared and boggle letter sets are logged correctly.