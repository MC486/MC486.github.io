#TODO
test_naive_bayes_word_feature_weights() #Confirms that features (letter positions, categories) are weighted and learned.

test_naive_bayes_probability_distribution() #Validates likelihood estimates under different feature conditions.

test_naive_bayes_prediction_constraint_handling() #Ensures the model doesn’t predict impossible words (e.g., ones using letters not present).